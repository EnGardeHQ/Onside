# OnSide Frontend Architecture

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         OnSide Platform                          │
│                   Competitive Intelligence System                │
└─────────────────────────────────────────────────────────────────┘

                              │
                              │
        ┌─────────────────────┴─────────────────────┐
        │                                           │
        ▼                                           ▼
┌────────────────┐                         ┌────────────────┐
│   Streamlit    │                         │  React Web     │
│     Demo       │                         │  Application   │
│   Dashboard    │                         │   (Frontend)   │
└────────────────┘                         └────────────────┘
        │                                           │
        │                                           │
        │        ┌──────────────────────────┐      │
        └────────►   FastAPI Backend        ◄──────┘
                 │   (Python)               │
                 └──────────────────────────┘
                          │      │
                          │      │
                    ┌─────┘      └─────┐
                    ▼                  ▼
            ┌──────────────┐   ┌──────────────┐
            │  PostgreSQL  │   │  WebSocket   │
            │   Database   │   │    Server    │
            └──────────────┘   └──────────────┘
```

---

## Frontend Component Architecture

### React Application Structure

```
frontend/
│
├── public/                          # Static assets
│
├── src/
│   ├── main.tsx                    # Application entry point
│   ├── index.css                   # Global styles
│   │
│   ├── components/                 # React components
│   │   │
│   │   ├── ProgressTracker/        # ✅ IMPLEMENTED
│   │   │   ├── index.ts           # Component exports
│   │   │   ├── ProgressBar.tsx    # Progress bar component
│   │   │   ├── ProgressBar.css
│   │   │   ├── StageIndicator.tsx # Stage visualization
│   │   │   ├── StageIndicator.css
│   │   │   ├── ReportProgressTracker.tsx  # Main component
│   │   │   ├── ReportProgressTracker.css
│   │   │   └── ProgressTracker.example.tsx
│   │   │
│   │   ├── Auth/                   # 🚧 TODO - Issue #31
│   │   │   ├── LoginForm.tsx
│   │   │   ├── RegisterForm.tsx
│   │   │   └── OAuthButtons.tsx
│   │   │
│   │   ├── Layout/                 # 🚧 TODO - Issue #31
│   │   │   ├── Header.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── MainLayout.tsx
│   │   │
│   │   ├── Dashboard/              # 🚧 TODO - Issue #31
│   │   │   ├── MetricsCard.tsx
│   │   │   ├── RecentActivity.tsx
│   │   │   └── QuickActions.tsx
│   │   │
│   │   ├── Competitors/            # 🚧 TODO - Issue #33
│   │   │   ├── CompetitorList.tsx
│   │   │   ├── CompetitorForm.tsx
│   │   │   ├── CompetitorCard.tsx
│   │   │   └── ComparisonView.tsx
│   │   │
│   │   ├── Reports/                # 🚧 TODO - Issue #32
│   │   │   ├── ReportsList.tsx
│   │   │   ├── ReportDetail.tsx
│   │   │   ├── ReportCharts.tsx
│   │   │   └── ReportExport.tsx
│   │   │
│   │   └── SEO/                    # 🚧 TODO - Issue #34
│   │       ├── KeywordRankings.tsx
│   │       ├── PageSpeedMetrics.tsx
│   │       ├── SERPFeatures.tsx
│   │       └── TrendCharts.tsx
│   │
│   ├── hooks/                      # Custom React hooks
│   │   ├── useProgressTracking.ts # ✅ IMPLEMENTED
│   │   ├── useAuth.ts             # 🚧 TODO
│   │   ├── useCompetitors.ts      # 🚧 TODO
│   │   ├── useReports.ts          # 🚧 TODO
│   │   └── useSEO.ts              # 🚧 TODO
│   │
│   ├── api/                        # API client layer
│   │   ├── client.ts              # 🚧 TODO - Axios instance
│   │   ├── auth.ts                # 🚧 TODO - Auth endpoints
│   │   ├── competitors.ts         # 🚧 TODO - Competitor endpoints
│   │   ├── reports.ts             # 🚧 TODO - Report endpoints
│   │   └── seo.ts                 # 🚧 TODO - SEO endpoints
│   │
│   ├── store/                      # State management
│   │   ├── authStore.ts           # 🚧 TODO - Zustand
│   │   ├── competitorStore.ts     # 🚧 TODO - Zustand
│   │   └── uiStore.ts             # 🚧 TODO - Zustand
│   │
│   ├── utils/                      # Utility functions
│   │   ├── formatting.ts          # 🚧 TODO
│   │   ├── validation.ts          # 🚧 TODO
│   │   └── helpers.ts             # 🚧 TODO
│   │
│   └── types/                      # TypeScript types
│       ├── api.ts                 # 🚧 TODO
│       ├── competitor.ts          # 🚧 TODO
│       ├── report.ts              # 🚧 TODO
│       └── user.ts                # 🚧 TODO
│
├── package.json                    # ✅ Dependencies
├── vite.config.ts                 # ✅ Build config
├── tsconfig.json                  # ✅ TypeScript config
├── .env.example                   # ✅ Environment template
└── README.md                      # ✅ Documentation
```

---

## WebSocket Progress Tracking Flow

```
┌──────────────┐
│   Browser    │
│  (React UI)  │
└──────┬───────┘
       │
       │ 1. User clicks "Generate Report"
       │
       ▼
┌─────────────────────────────────────────┐
│  ReportProgressTracker Component        │
│  - Renders UI                           │
│  - Manages user interactions            │
└──────┬──────────────────────────────────┘
       │
       │ 2. Calls useProgressTracking hook
       │
       ▼
┌─────────────────────────────────────────┐
│  useProgressTracking Hook                │
│  - Creates WebSocket connection         │
│  - Manages connection state             │
│  - Handles reconnection                 │
└──────┬──────────────────────────────────┘
       │
       │ 3. Opens WebSocket
       │
       ▼
┌─────────────────────────────────────────┐
│  WebSocket Connection                    │
│  ws://localhost:8000/api/v1/progress/   │
│  ws/{reportId}?user_id={userId}         │
└──────┬──────────────────────────────────┘
       │
       │ 4. Backend sends progress updates
       │
       ▼
┌─────────────────────────────────────────┐
│  Progress Updates (JSON)                 │
│  {                                       │
│    status: "in_progress",               │
│    currentStage: "competitor_analysis", │
│    overallProgress: 35,                 │
│    stageProgress: {...},                │
│    estimatedTimeRemaining: 120          │
│  }                                       │
└──────┬──────────────────────────────────┘
       │
       │ 5. Hook updates state
       │
       ▼
┌─────────────────────────────────────────┐
│  Component Re-renders                    │
│  - Updates progress bar                 │
│  - Updates stage indicators             │
│  - Shows time estimate                  │
└─────────────────────────────────────────┘
```

---

## Data Flow Architecture

### State Management Strategy

```
┌────────────────────────────────────────────────────┐
│                  Application State                  │
└────────────────────────────────────────────────────┘
              │                    │
              │                    │
    ┌─────────▼─────────┐  ┌──────▼───────┐
    │  Server State     │  │ Client State │
    │  (React Query)    │  │  (Zustand)   │
    └───────────────────┘  └──────────────┘
              │                    │
    ┌─────────▼─────────┐  ┌──────▼───────┐
    │ - User data       │  │ - UI state   │
    │ - Competitors     │  │ - Theme      │
    │ - Reports         │  │ - Sidebar    │
    │ - SEO data        │  │ - Modals     │
    │ - Analytics       │  │ - Filters    │
    └───────────────────┘  └──────────────┘
```

### API Integration Pattern

```
Component
   │
   │ 1. Uses custom hook
   ▼
useCompetitors()
   │
   │ 2. React Query hook
   ▼
useQuery('competitors')
   │
   │ 3. API client
   ▼
api.competitors.list()
   │
   │ 4. HTTP Request
   ▼
GET /api/v1/competitors
   │
   │ 5. FastAPI Backend
   ▼
PostgreSQL Database
```

---

## Component Hierarchy

### Main Application Structure

```
<App>
  │
  ├── <AuthProvider>              # Authentication context
  │   │
  │   └── <Router>                # React Router
  │       │
  │       ├── <PublicRoutes>
  │       │   ├── /login  → <LoginPage>
  │       │   └── /register → <RegisterPage>
  │       │
  │       └── <ProtectedRoutes>
  │           │
  │           └── <MainLayout>
  │               ├── <Header>
  │               ├── <Sidebar>
  │               │
  │               └── <Content>
  │                   │
  │                   ├── /dashboard → <Dashboard>
  │                   │   ├── <MetricsCard>
  │                   │   ├── <RecentActivity>
  │                   │   └── <QuickActions>
  │                   │
  │                   ├── /competitors → <CompetitorsPage>
  │                   │   ├── <CompetitorList>
  │                   │   ├── <CompetitorForm>
  │                   │   └── <ComparisonView>
  │                   │
  │                   ├── /reports → <ReportsPage>
  │                   │   ├── <ReportsList>
  │                   │   ├── <ReportDetail>
  │                   │   ├── <ReportProgressTracker> ✅
  │                   │   └── <ReportCharts>
  │                   │
  │                   └── /seo → <SEOPage>
  │                       ├── <KeywordRankings>
  │                       ├── <PageSpeedMetrics>
  │                       └── <TrendCharts>
  │
  └── <Notifications>            # Toast notifications
```

---

## Streamlit Dashboard Architecture

```
streamlit_dashboard.py
│
├── Configuration
│   ├── Environment variables
│   ├── API base URL
│   └── Demo mode flag
│
├── Session State
│   ├── authenticated
│   ├── api_token
│   └── selected_competitors
│
├── Helper Functions
│   ├── make_api_request()
│   └── Data processing
│
├── Page Components
│   │
│   ├── login_page()
│   │   ├── Login form
│   │   └── Demo mode button
│   │
│   ├── sidebar_navigation()
│   │   ├── Navigation radio
│   │   ├── Date range filter
│   │   └── Competitor selection
│   │
│   ├── dashboard_overview()
│   │   ├── Key metrics
│   │   ├── Competitor charts
│   │   └── Recent activity
│   │
│   ├── competitor_analysis()
│   │   ├── SEO rankings
│   │   ├── Content analysis
│   │   └── Tech stack
│   │
│   ├── seo_analytics()
│   │   ├── Keyword trends
│   │   ├── Core Web Vitals
│   │   └── PageSpeed scores
│   │
│   ├── reports_view()
│   │   ├── Reports list
│   │   ├── Generation UI
│   │   └── Export options
│   │
│   └── settings_page()
│       ├── API config
│       ├── Notifications
│       └── Export settings
│
└── Main Entry Point
    └── main() → Route to pages
```

---

## Technology Stack Decisions

### Current Stack ✅

```
┌──────────────────────────────────────┐
│         Frontend Layer               │
│  ┌────────────┐  ┌────────────┐    │
│  │  Streamlit │  │   React    │    │
│  │   1.29.0   │  │   18.2.0   │    │
│  └────────────┘  └────────────┘    │
│                                      │
│  ┌────────────┐  ┌────────────┐    │
│  │   Plotly   │  │ TypeScript │    │
│  │   5.18.0   │  │   5.3.3    │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│         Build Layer                   │
│  ┌────────────┐  ┌────────────┐    │
│  │    Vite    │  │   ESBuild  │    │
│  │   5.0.8    │  │  (bundled) │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│         API Layer                     │
│  ┌────────────┐  ┌────────────┐    │
│  │  FastAPI   │  │ WebSocket  │    │
│  │   0.104.1  │  │   Native   │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│       Database Layer                  │
│  ┌────────────┐  ┌────────────┐    │
│  │ PostgreSQL │  │ SQLAlchemy │    │
│  │    15.x    │  │   2.0.23   │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
```

### Planned Additions 🚧

```
┌──────────────────────────────────────┐
│      State Management                 │
│  ┌────────────┐  ┌────────────┐    │
│  │ React Query│  │  Zustand   │    │
│  │   (TBD)    │  │   (TBD)    │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│        UI Components                  │
│  ┌────────────┐  ┌────────────┐    │
│  │ Headless UI│  │  Tailwind  │    │
│  │    (TBD)   │  │    (TBD)   │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│      Visualization                    │
│  ┌────────────┐  ┌────────────┐    │
│  │  Recharts  │  │    D3.js   │    │
│  │    (TBD)   │  │   (TBD)    │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│         Forms                         │
│  ┌────────────┐  ┌────────────┐    │
│  │React Hook  │  │    Zod     │    │
│  │    Form    │  │ Validation │    │
│  │    (TBD)   │  │   (TBD)    │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
```

---

## Deployment Architecture

### Development Environment

```
Developer Machine
│
├── Streamlit (Port 8501)
│   └── streamlit run streamlit_dashboard.py
│
├── React Dev Server (Port 3000)
│   └── npm run dev (Vite)
│
└── Backend API (Port 8000)
    ├── FastAPI server
    └── WebSocket server
```

### Production Architecture (Proposed)

```
┌─────────────────────────────────────────┐
│           Load Balancer / CDN            │
│              (CloudFlare)                │
└──────────┬──────────────────────┬────────┘
           │                      │
           ▼                      ▼
┌──────────────────┐    ┌──────────────────┐
│  Static Hosting  │    │   API Server     │
│   (Vercel/       │    │   (Railway/      │
│    Netlify)      │    │    AWS/GCP)      │
│                  │    │                  │
│  React App       │    │  FastAPI         │
│  (HTML/CSS/JS)   │    │  + WebSocket     │
└──────────────────┘    └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │   PostgreSQL     │
                        │   (Managed)      │
                        └──────────────────┘
```

---

## Security Architecture

### Authentication Flow

```
┌──────────────┐
│    Client    │
└──────┬───────┘
       │
       │ 1. POST /auth/login
       │    { username, password }
       ▼
┌──────────────────┐
│  FastAPI Auth    │
│    Endpoint      │
└──────┬───────────┘
       │
       │ 2. Validate credentials
       ▼
┌──────────────────┐
│   PostgreSQL     │
│   Users Table    │
└──────┬───────────┘
       │
       │ 3. Generate JWT
       ▼
┌──────────────────┐
│  JWT Token       │
│  + User Data     │
└──────┬───────────┘
       │
       │ 4. Return with HttpOnly cookie
       ▼
┌──────────────────┐
│    Client        │
│  (Token stored   │
│   in cookie)     │
└──────────────────┘
```

### API Request Flow with Auth

```
┌──────────────┐
│    Client    │
└──────┬───────┘
       │
       │ GET /api/v1/competitors
       │ Cookie: jwt=token
       ▼
┌──────────────────┐
│  API Middleware  │
│  - Extract JWT   │
│  - Validate      │
└──────┬───────────┘
       │
       │ Valid token?
       ▼
┌──────────────────┐
│  Route Handler   │
│  - Process req   │
│  - Query DB      │
└──────┬───────────┘
       │
       │ Return data
       ▼
┌──────────────────┐
│    Client        │
│  (Receives data) │
└──────────────────┘
```

---

## Performance Optimization Strategy

### Code Splitting

```
Initial Bundle
├── Vendor chunks
│   ├── react-vendor.js
│   ├── charts-vendor.js
│   └── query-vendor.js
│
├── Route chunks
│   ├── dashboard.chunk.js
│   ├── competitors.chunk.js
│   ├── reports.chunk.js
│   └── seo.chunk.js
│
└── Component chunks
    ├── ProgressTracker.chunk.js
    └── ReportCharts.chunk.js
```

### Caching Strategy

```
┌──────────────────────────────────────┐
│           Browser Cache               │
│  ┌────────────┐  ┌────────────┐    │
│  │Static Asset│  │   Images   │    │
│  │ (1 year)   │  │  (1 year)  │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│         React Query Cache             │
│  ┌────────────┐  ┌────────────┐    │
│  │Competitors │  │  Reports   │    │
│  │  (5 min)   │  │  (2 min)   │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘

┌──────────────────────────────────────┐
│          CDN Cache                    │
│  ┌────────────┐  ┌────────────┐    │
│  │  JS/CSS    │  │  API Data  │    │
│  │ (1 week)   │  │  (1 hour)  │    │
│  └────────────┘  └────────────┘    │
└──────────────────────────────────────┘
```

---

## Accessibility Architecture

### WCAG 2.1 AA Compliance

```
┌──────────────────────────────────────┐
│      Semantic HTML Layer              │
│  - <header>, <nav>, <main>           │
│  - <article>, <section>              │
│  - <button>, <input>, <label>        │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│       ARIA Attributes Layer           │
│  - aria-label, aria-describedby      │
│  - role="progressbar"                │
│  - aria-live regions                 │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│     Keyboard Navigation Layer         │
│  - Tab order                         │
│  - Focus management                  │
│  - Keyboard shortcuts                │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│       Screen Reader Layer             │
│  - Announcements                     │
│  - Status updates                    │
│  - Context information               │
└──────────────────────────────────────┘
```

---

## Testing Architecture (Proposed)

```
┌──────────────────────────────────────┐
│          Unit Tests (Jest)            │
│  - Component logic                   │
│  - Hook behavior                     │
│  - Utility functions                 │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│   Integration Tests (React Testing   │
│          Library)                     │
│  - Component interactions            │
│  - Form submissions                  │
│  - API integration                   │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│      E2E Tests (Playwright)           │
│  - User flows                        │
│  - Critical paths                    │
│  - Cross-browser                     │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│   Accessibility Tests (axe-core)      │
│  - WCAG compliance                   │
│  - ARIA validation                   │
│  - Keyboard navigation               │
└──────────────────────────────────────┘
```

---

## Monitoring & Analytics (Proposed)

```
┌──────────────────────────────────────┐
│      Performance Monitoring           │
│  - Lighthouse CI                     │
│  - Web Vitals                        │
│  - Bundle size tracking              │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│       Error Tracking                  │
│  - Sentry / LogRocket                │
│  - Error boundaries                  │
│  - Source maps                       │
└──────────────────────────────────────┘
                 │
                 ▼
┌──────────────────────────────────────┐
│      User Analytics                   │
│  - Google Analytics 4                │
│  - User behavior tracking            │
│  - Feature usage                     │
└──────────────────────────────────────┘
```

---

## Summary

This architecture provides:

✅ **Scalability**: Modular component structure
✅ **Maintainability**: Clear separation of concerns
✅ **Performance**: Code splitting and caching
✅ **Accessibility**: WCAG 2.1 AA compliance
✅ **Security**: JWT authentication, HTTPS
✅ **Developer Experience**: TypeScript, hot reload
✅ **User Experience**: Real-time updates, responsive design

**Status**: Phase 1 architecture complete, ready for expansion!
