name: CD - Deploy to Staging

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.11"
  DOCKER_REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}/onside-api

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tags }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha,prefix=staging-
            type=raw,value=staging-latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          target: production
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          build-args: |
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
            VCS_REF=${{ github.sha }}
            VERSION=staging-${{ github.sha }}

  deploy-staging:
    name: Deploy to Staging Environment
    runs-on: ubuntu-latest
    needs: build-and-push
    environment:
      name: staging
      url: https://staging.onside.app
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      # ECS Deployment
      - name: Update ECS Task Definition
        id: task-def
        run: |
          # TODO: Update this with actual ECS task definition update
          echo "Updating ECS task definition with image: ${{ needs.build-and-push.outputs.image-tag }}"
          # aws ecs update-service --cluster onside-staging --service onside-api --force-new-deployment

      # Alternative: Railway Deployment
      - name: Deploy to Railway (Staging)
        if: false  # Enable if using Railway instead of AWS
        uses: bervProject/railway-deploy@main
        with:
          railway_token: ${{ secrets.RAILWAY_TOKEN }}
          service: onside-api-staging
          environment: staging

      # Database Migrations
      - name: Run Database Migrations
        run: |
          echo "Running database migrations..."
          # TODO: Implement database migration execution
          # kubectl exec -it deployment/onside-api -- alembic upgrade head

      - name: Deployment notification
        run: |
          echo "Deployment to staging completed"
          # TODO: Add notification to Slack/Discord

  smoke-tests:
    name: Run Smoke Tests
    runs-on: ubuntu-latest
    needs: deploy-staging
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install test dependencies
        run: |
          pip install requests pytest

      - name: Wait for deployment
        run: sleep 30

      - name: Run smoke tests
        env:
          STAGING_URL: https://staging.onside.app
          API_KEY: ${{ secrets.STAGING_API_KEY }}
        run: |
          # Health check
          curl -f $STAGING_URL/health || exit 1

          # API smoke tests
          # pytest tests/smoke/ --base-url=$STAGING_URL
          echo "Smoke tests passed"

      - name: Rollback on failure
        if: failure()
        run: |
          echo "Smoke tests failed, initiating rollback..."
          # TODO: Implement rollback logic
          # aws ecs update-service --cluster onside-staging --service onside-api --task-definition previous-revision

  notify:
    name: Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [build-and-push, deploy-staging, smoke-tests]
    if: always()
    steps:
      - name: Send notification
        run: |
          echo "Staging deployment completed with status: ${{ job.status }}"
          # TODO: Send notification to Slack/Discord/Email
